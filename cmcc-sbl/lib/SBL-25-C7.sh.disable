#!/bin/sh
#
#  允许Ctrl+Alt+Delete重新启动机器
#  
#  编辑“/etc/inittab”文件，将ca::ctrlaltdel:/sbin/shutdown -t3 -r now注释：
#  #ca::ctrlaltdel:/sbin/shutdown -t3 -r now 
#  
#  然后，为使更改生效，在提示符下输入：
#  /sbin/init q

# RHEL6.x的配置文件是： /etc/init/control-alt-delete.conf
# 默认是启用状态，暂不操作
